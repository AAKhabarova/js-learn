"use strict";

console.log(4 + +'5');

let incr = 10,
    decr = 10;

// оператор инкремента (увеличение на 1)
++incr;
// оператор декримента (уменьшение на 1)
--decr;

console.log(incr);
console.log(decr);

// Префиксные формы операторов:
// ++incr;
// --decr;

// Постфиксные формы операторов:
// incr++;
// decr--;
